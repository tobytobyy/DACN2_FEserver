// Re-export DailyMetrics as the calendar day data shape.
// HealthSummary (the old string-based type) has been removed — use DailyMetrics directly.
export type { DailyMetrics } from '../../types/home';
