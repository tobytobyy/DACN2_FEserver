import React from 'react';

type Props = { visible: boolean; onClose: () => void; onSaved: () => void };

// Placeholder — full implementation lands in Task 10.
const LogSleepModal: React.FC<Props> = () => null;

export default LogSleepModal;
